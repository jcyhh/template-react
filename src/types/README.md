# Types
共享类型。

This directory stores TypeScript types that are shared across multiple business modules.
这个目录存放跨多个业务模块复用的 TypeScript 类型。

Keep feature-only response types inside the corresponding feature directory.
只属于某个 feature 的接口响应类型应保留在对应 feature 目录中。
