# Popup showcase
Popup 组件演示页。

This development-only secondary page opens the shared `Popup` and keeps its own display content outside the component source.
这个仅开发环境使用的二级页面会打开通用 `Popup`，展示内容保留在演示页自身中，不修改组件源码。
