# Picker showcase
Picker 组件演示页。

This development-only secondary page demonstrates the shared `Picker` with normal options, custom option rendering and empty data.
这个仅开发环境使用的二级页面用于演示通用 `Picker` 的普通选项、自定义选项渲染和空数据状态。
